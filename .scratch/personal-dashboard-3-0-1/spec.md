# Personal Dashboard 3.0.1 polish

This bounded patch keeps the Dashboard 4 shell and task semantics unchanged while addressing three acceptance areas:

- Add meaningful English display names to the selected Vault's existing Habit sidecar without changing source names, stable keys, goals, history, Daily Records, snapshots, Dida365 data, or automation.
- Make New Task a theme-colored primary action with readable white text and interaction states; keep Refresh Tasks secondary and high contrast, with at least 4.5:1 enabled text contrast across forest, blue, clay, and lilac.
- Define coherent accent, strong, soft, on-accent, and focus roles for all four existing palettes and use them for brand consumers across the shell, destinations, actions, Settings, selected controls, links, and focus indicators while preserving semantic colors, background images, and swatches.

Version package declarations and applicable lock metadata to 3.0.1 without dependency upgrades; preserve the bundle identifier. Validate focused regressions, full automated checks, rendered wide/narrow Chinese/English surfaces, computed contrast, and isolated packaged persistence. Do not install or replace the stable `/Applications` app, push, merge, or close unrelated Drive acceptance.
