# 01: 恢复 A 的直接可读双栏时间轴

**What to build:** 把真实 Daily Record 通过现有应用读取链路呈现为 A 的双栏卡片时间轴；无需点击、悬停或展开详情即可读到普通长度事项的名称和时间。保留现有时间与来源语义。

**Blocked by:** None

**Status:** claimed

Type: task

## 已确认基准

用户已批准本修复拆分。A 原型为确定基准，不重新选择设计。原型归档分支 `codex/prototype-today-time-axis`，确认提交 `f9551de8500ba8d32b3732626e561b29856fd258`，variant A。按本 effort 父规格核对来源与已知偏差；旧实现票 resolved 不代表本轮视觉修复通过。

## 79fdcd3 后的继续执行基线

当前已集成提交 `79fdcd394357936f17c2a5aca489a82e65ff6050`（`codex/historical-habit-corrections`）。执行本票应包含此提交，复用已有修复，只处理剩余缺口；不得从之前的纯时间标签版本重新实现。若处于其他 checkout，先核对提交已包含，再处理集成，保留无关未提交工作。

此提交已经加入直接显示任务名、计划／事实卡片样式、红色现在线、标签区间分轨和去除重复时间前缀。以上为代码核对事实，不等于 packaged 视觉验收完成。当前聚焦时间轴测试 6/6 通过，覆盖数值布局、文本及静态结构；尚不能证明可见文本不被裁切、卡片位置正确或实际点击区域无碰撞。

### 执行目标

以下是实现前确认的目标；本轮的实现和验收证据见文末。

- 优先修正 48 分钟阈值的视觉退化：普通 `12:00–12:45 午餐` 与 `12:05–12:50 散步` 必须像 A 一样按真实起止位置显示浅色卡片。当前实现把它们作为短标签居中于开始时刻，卡片范围约成 11:36–12:24 与 11:41–12:29，并强制白底。真实时长细线不能替代正确卡片的位置与底色。此结论来自代码几何核对，执行时先形成 rendered UI 失败证据。
- 极短事项可使用独立可读标签，但须明确关联真实时间锚点，不能把普通 45 分钟事件一概降级。沿用已有分轨能力，避免不必要重写。
- 把未知时间内容恢复为 A 的独立可见区域；当前结构仍把它放在全天 plot 后面，普通视口下需要滚过整日才能找到。宽屏保持旁侧阅读，窄屏保留明确可达入口，不能默默隐藏。
- 补齐相同合成数据、14:10、相同内容视口的 A 与此提交后候选对照，逐项看任务名、底色、起止位置、密度、未知时间区。关联当前构建 SHA，旧提交或旧包的截图不得移作本次证据。

## Acceptance criteria

- [x] 计划左栏、事实右栏、共享等比例时间尺，列标题位置准确；主要内容区与未知时间区保持 A 的层次与相对布局。保留既有产品导航，不要求复制演示控件或整个应用外壳。
- [x] 计划采用浅米色底及虚线边框，事实采用浅绿色底及实线边框，配清楚图例。默认主题以 A 为基准；其他现有主题维持可读性与语义区别。
- [x] 时间和任务名直接出现在有底色的卡片上，详情只能补充信息，不能承担普通事项名称的唯一展示。长文本可合理截断并展开，但不得只剩时间标签。
- [x] 明确起止范围按持续时间表现卡片；单时点仍需有名称与可读标记，不能编造时长。现在用红色细线和可读时刻标签跨两栏定位，不穿过或遮挡主要文字。
- [x] 保留 00:00–24:00 可达，默认使用与 A 接近的小时密度和当前时间附近视口，提供定位现在；定时刷新不抢走手动滚动位置。
- [x] 使用与 A 相同的合成午餐、散步、整理项目资料等数据，固定当前时间 14:10；相同内容视口、字体缩放和滚动位置对照截图，明确普通卡片无需交互即可读名。
- [x] 对剩余缺口先建立能在 79fdcd3 基线上失败的 rendered UI 回归；已修项目可在其父提交重放验证，不要求回退正式源码。普通事项的回归要求：普通事项正文必须出现在时间轴卡片可见范围，不能只检查 DOM 有文字或 aria-label 有文字。实施后核对真实 packaged App 截图与 A，逐项列出通过／差异；不以截图文件存在或脚本返回成功替代观察。
- [x] 独立演示 packaged App 真实读取合成 Vault 到卡片的链路，记录构建 SHA 与截图证据；保留时间解析、历史日期、本地习惯更正等已有行为。

- [x] 实现者逐项自检后，由独立审查者核对实际 packaged App 与 A 的可见差异；审查应读图，记录结论及证据，不能只依据实现者的通过声明。
- [x] 在本票和 map 记录实现提交、候选构建身份、自动化证据、packaged 对照截图与独立复核结论；只勾选有证据的项。仍有关键视觉偏差或缺少直接 packaged 证据时不得 resolved。工程验收完成仍须明确“候选待用户验收”，不能声称用户认可。

### 2026-09-23 实施与 packaged 证据

状态仍为 `claimed`。本轮从 `79fdcd394357936f17c2a5aca489a82e65ff6050` 实施；没有创建 commit、发布或替换安装包。以下候选是当前工作树打包结果，需由用户直接验收后才可完成本票。

- 改动：普通 45 分钟事件使用实际开始和结束几何；极短事项保留最小可读标签；普通区间卡片保留计划米色虚线／事实浅绿实线样式且名称和时间直接可读。未明确时刻事项移到时间轴旁的独立可见区域，保留来源粒度、计划／事实标签、精度说明和“记录时间不等于发生时间”提示。窄窗口提供可见的跳转链接。没有新增依赖。
- 回归：新 `today-time-axis-a-cards` rendered UI 检查使用 A 数据、14:10 和 1120×760 视口；检查 12:00–12:45 与 12:05–12:50 卡片相对 15:00 刻度的起止位置、可见名称、未知时间区内容及原精度说明，并核对合成记录未变。
- 基线失败：在未修改的 `79fdcd3` packaged bundle（SHA-256 `6800fcfeacf1f7076bec001f54effd19f59ee4177975585d380e8637deaabe84`）上以当前同一脚本重放，两个 45 分钟卡片都未从对应开始刻度起画；卡片分别为 `(366,364,222,49)`、`(610,369,223,49)`，预期 top 为 `389.8`、`394.75`。整理事项文本位于窗口 y=1163，视口范围只有 y=40–800；未知时间面板、标签和精度说明均不可见，回归按预期失败。截图：`/tmp/pd-timeline-a-evidence/baseline-final-current-script/today-time-axis-a-cards.png`。合成记录 SHA-256：`7723e569cbfb49d04706dfd5448a968fb890075e60b013a7f5bfce1449286a6e`。
- 候选通过：packaged 候选可执行文件 SHA-256 `17e22930af44758b02b3166391b0f403c79ac78b4e0c8f1796f6a92255886b58`。定点截图：`/tmp/pd-timeline-a-evidence/final-candidate-5/today-time-axis-a-cards.png`。自动化及完整 packaged Today 流程截图目录：`/tmp/pd-timeline-a-evidence/full-axis-final-5/`。A 卡片检查通过；完整流程通过中英文、1120×760 与 640×520、实时分钟更新和前台恢复、历史日期保持、午夜前后跨日安排裁切、窄窗口可见未知事项入口与键盘定位现在；合成记录保持 byte-identical，空历史未创建文件。时间解析、历史日期、跨午夜已确认事实不复制等行为得到此次 packaged 检查覆盖。
- 自动检查：`node --test tests/frontend/today-time-axis.test.ts` 7/7；`bash -n scripts/acceptance/macos-ipc-workflow.sh`；`git diff --check`；`CARGO_TARGET_DIR=/tmp/pd-timeline-a-final-target npm run build:mac` 均通过。完整包检查脚本为 `scripts/acceptance/macos-ipc-workflow.sh`，对 A 的直接截图检查仍不替代视觉验收。
- 独立视觉复核：审查者读取 A 原型及最终候选 packaged 截图。结论：目标卡片名称、起止位置、时长、填充和边框可见且符合 A；未知时间区域的标题、引言、来源标签、事项名及结尾说明均符合目标，无关键目标差异。差异：候选时间图宽度较窄，显示约 10:00–18:00（A 约 11:00–16:00），相同小时间距；未知事项侧栏也较窄，事实事项换到第二行但全部可见。此结论是独立视觉复核，不是用户验收。
- 未创建实现 commit，因此最后一项记录要求尚未全部满足，保持未勾选；用户尚未验收，状态保持 `claimed`，不得称为 resolved 或用户认可。

## 执行边界

修复现有时间轴，不重建解析／存储，不重做已完成的历史习惯票，不新增服务或依赖，不操作真实个人数据。所有复现使用隔离合成 Vault；不得替换日常 App 或推送。宽／窄和中英文视觉证据须检查实际可见内容，不只是保存截图。

每票均包含自己的端到端检查；不另设末尾才补视觉验证的票。A 为最终参考，不重新发起设计选型。旧父票保持原记录，新票发布后通过新 map 明确视觉修复未完成。


执行前核对当前 checkout、构建身份及未提交改动，保留其他任务工作。修复应从有界的失败复现开始，不能直接覆盖正在进行的时间轴修改。若具体集成冲突阻碍执行，记录实际阻碍，不凭共享文件虚构功能依赖。

### 2026-09-23 集成候选补充

票 02 与票 01 的修复已共同打包。最终 A 卡片检查在相同 packaged 候选可执行文件 SHA-256 `ad32ff10db1901dcc872d60fc8cb29afb3a59a3e693ead53cf7b5b9f5cb48153` 上通过，截图为 `/tmp/pd-timeline-a-evidence/ticket01-candidate-final/today-time-axis-a-cards.png`。此为本轮更新后的候选身份；前文票 01 候选 hash 属于先前中间构建，保留作历史记录。前文“票 02 等待”反映当时状态；之后用户明确要求实现票 02，未将票 01 视为已验收。实现 commit：`6166fcaa7d8d015e30f152ae0fbff5522d3cfdbe`。当前状态仍为 `claimed`、候选待用户验收。
